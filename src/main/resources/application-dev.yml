spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/talci_dev?serverTimezone=UTC
    username: talci_dev_user
    password: talci
    initialization-mode: always
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL8Dialect
    database: mysql
    show-sql: true
#    properties:
#          javax:
#            persistence:
#              schema-generation:
#                create-source: metadata
#                scripts:
#                  action: create
#                  create-target: talci_database_create.sql
